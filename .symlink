# .symlink format
# .symlink in a directory tells symlink which executable 
# files must be symlinked to /usr/local/bin
# when using 'symlink -SP'

# The symlink program itself
symlink

# and add any other programs
# ...
